<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Pulsemap.App.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource Gray50Brush}">

    <Grid Padding="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Uid="SettingsTitle" Grid.Row="0" FontSize="{StaticResource FontSize2xl}" FontWeight="SemiBold" Foreground="{StaticResource Gray900Brush}" />

        <Border Grid.Row="1" Margin="0,24,0,0" Background="White" CornerRadius="16" BorderBrush="{StaticResource Gray200Brush}" BorderThickness="1" Padding="24" Width="400" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel Spacing="12">
                <TextBlock x:Uid="SettingsLanguageLabel" FontWeight="SemiBold" FontSize="{StaticResource FontSizeSm}" Foreground="{StaticResource Gray900Brush}" />
                <TextBlock Text="{x:Bind ViewModel.CurrentLanguageDisplay}" FontSize="{StaticResource FontSizeSm}" Foreground="{StaticResource Gray700Brush}" />
                <TextBlock
                    x:Uid="SettingsLanguageExplanation"
                    TextWrapping="Wrap"
                    FontSize="{StaticResource FontSizeXs}"
                    Foreground="{StaticResource Gray500Brush}" />
                <Button
                    x:Uid="SettingsOpenLanguageSettingsButton"
                    Command="{x:Bind ViewModel.OpenLanguageSettingsCommand}" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Margin="0,16,0,0" Background="White" CornerRadius="16" BorderBrush="{StaticResource Gray200Brush}" BorderThickness="1" Padding="24" Width="400" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel Spacing="12">
                <TextBlock x:Uid="SettingsDiagnosticsLabel" FontWeight="SemiBold" FontSize="{StaticResource FontSizeSm}" Foreground="{StaticResource Gray900Brush}" />
                <TextBlock Text="{x:Bind ViewModel.LogDirectoryDisplay}" TextWrapping="Wrap" FontSize="{StaticResource FontSizeSm}" Foreground="{StaticResource Gray700Brush}" />
                <TextBlock
                    x:Uid="SettingsDiagnosticsExplanation"
                    TextWrapping="Wrap"
                    FontSize="{StaticResource FontSizeXs}"
                    Foreground="{StaticResource Gray500Brush}" />
                <Button
                    x:Uid="SettingsOpenLogsFolderButton"
                    Command="{x:Bind ViewModel.OpenLogsFolderCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
