<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Pulsemap.App.Views.SnapshotComparisonPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Pulsemap.App.Controls"
    mc:Ignorable="d"
    Background="{StaticResource Gray50Brush}">

    <Grid Padding="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Uid="SnapshotComparisonTitle" Grid.Column="0" FontSize="{StaticResource FontSize2xl}" FontWeight="SemiBold" Foreground="{StaticResource Gray900Brush}" />
            <Button x:Uid="SnapshotComparisonBackButton" Grid.Column="1" Click="Back_Click" />
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="16" Margin="0,20,0,0" VerticalAlignment="Bottom">
            <ComboBox
                x:Uid="SnapshotComparisonLeftField"
                Width="180"
                ItemsSource="{x:Bind ViewModel.Options, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.LeftOption, Mode=TwoWay}"
                DisplayMemberPath="Label" />
            <ComboBox
                x:Uid="SnapshotComparisonRightField"
                Width="180"
                ItemsSource="{x:Bind ViewModel.Options, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.RightOption, Mode=TwoWay}"
                DisplayMemberPath="Label" />
            <ComboBox
                x:Uid="SnapshotComparisonFloorField"
                Width="160"
                ItemsSource="{x:Bind ViewModel.Floors, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedFloor, Mode=TwoWay}"
                DisplayMemberPath="Name" />
            <ComboBox
                x:Uid="SnapshotComparisonBandField"
                Width="120"
                ItemsSource="{x:Bind ViewModel.AvailableBands, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedBand, Mode=TwoWay}" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,20,0,0" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" BorderBrush="{StaticResource Gray200Brush}" BorderThickness="1" CornerRadius="12">
                <controls:FloorPlanCanvas x:Name="LeftCanvas" />
            </Border>
            <Border Grid.Column="1" Background="White" BorderBrush="{StaticResource Gray200Brush}" BorderThickness="1" CornerRadius="12">
                <controls:FloorPlanCanvas x:Name="RightCanvas" />
            </Border>
        </Grid>
    </Grid>
</Page>
